#include <stdio.h>

/**
 *main will output single digits from 0 to 9
 *main function returns 0
 */
int main(void)
{
int number;

for (number = 0; number < 10; number++)
{
printf("%d", number);
}
printf("\n");
return (0);
}
